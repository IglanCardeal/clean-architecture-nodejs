version: '3'

services: 
  jenkins:
    image: jenkins/jenkins:lts
    privileged: true
    user: root
    ports:
      - 8083:8080
      - 50003:50000
    container_name: nodejs_ddd_tdd_ca-jenkins
    volumes:
      - ~/docker-volumes/ci/jenkins